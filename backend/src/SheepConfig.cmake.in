include(CMakeFindDependencyMacro)

# Capturing values from configure (optional)
#set(my-config-var @my-config-var@)

# Same syntax as find_package
find_dependency(HElib REQUIRED)
find_dependency(TFHE REQUIRED)
find_dependency(SEAL REQUIRED)
find_dependency(LP REQUIRED)
find_dependency(PALISADE REQUIRED)
find_dependency(TBB REQUIRED)
find_dependency(pthread REQUIRED)


# Any extra setup

# Add the targets file
include("${CMAKE_CURRENT_LIST_DIR}/SheepTargets.cmake")